---

# Playbook Ansible para construir cluster Kubernetes OnPrem 

- name: Configure Kubernetes cluster - OnPrem
  hosts: all
  gather_facts: true

  roles:
    - role: linux                      
    - role: k8s-packages                        
    - role: k8s-cluster-init           

...
