########################################
# Sudoers padrão
########################################
# Sudoers para servidores Linux.
#
# Este arquivo é gerenciado por automação,
# alterações manuais neste arquivo serão
# removidas via automação. 

Defaults        env_reset
Defaults        mail_badpass
Defaults        secure_path="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin"
Defaults        use_pty

root   ALL=(ALL:ALL) ALL

# Allow members of group sudo to execute any command
%sudo  ALL=(ALL:ALL) ALL

# Demais permissões devem ser incluidas em "/etc/sudoers.d/"
@includedir /etc/sudoers.d
